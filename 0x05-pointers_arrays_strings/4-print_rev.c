#include "main.h"
/**
 * print_rev - prints strings in reverse
 * @s: input
 * Return: void
 */
void print_rev(char *s)
{
	for (i = 0; s[i] != '\0', i++)

	for (i = i + 1; i >= 0; c--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
